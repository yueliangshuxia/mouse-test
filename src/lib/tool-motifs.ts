/**
 * 工具箱页每一件那一小块预览的图形。
 *
 * ## 为什么是一段内联 SVG,而不是截图或图片
 *
 * 一小块预览要能**跟着主题走**(纸面在浅色和深色下是两套令牌),而位图做不到
 * 这一点 —— 要么在两套寄存器下有一半是错的,要么得准备两份。所以图形一律
 * **`currentColor` 描边、不写死任何颜色**:它跟着所在的那块纸面走,一个令牌
 * 都不用新增。
 *
 * ## 为什么全是灰阶、一根彩色都不许有
 *
 * 参考图里那一排**彩色小圆点**是明确否掉的:纸面走无彩色是全站色彩纪律的
 * 支点 —— **有颜色的地方就是在出数**(只有仪器窗、警告、故障三处有颜色)。
 * 给预览窗上色,读者就得靠对比去判断"哪里是活的",那套一眼可见的分工当场失效。
 *
 * ## 形状
 *
 * 一律 `viewBox="0 0 24 24"`,描边宽度和圆角由**页面**统一给(见
 * `toolbox.astro` 的 `.box-item__preview svg`),这里只管画什么。
 *
 * **图形说的是"这件工具在做什么",不是装饰。** 坏点检测画四个格子填一个,
 * 因为那正是它让你找的东西;读数类的画一条波形,不画一个齿轮。
 *
 * 每个趣味工具都必须在这里有一条 —— `tests/nav.test.ts` 钉着这条。
 * 少一条的后果是那一格**静默空着**(`TOOL_MOTIF[slug]` 是 `undefined`,
 * 渲染成空白),而空白的预览窗看起来像"这一件还没做好"。
 */
export const TOOL_MOTIF: Record<string, string> = {
  /** 反应速度:一只秒表 —— 测的是"从变色到按下"的那一段。 */
  'reaction-test': `
    <circle cx="12" cy="13.5" r="7.5" />
    <path d="M12 9.5v4l2.8 2" />
    <path d="M9.5 3.5h5M12 3.5v2.5" />
  `,

  /** 瞄准:一个准星 —— 圆环加四根刻度。 */
  'aim-test': `
    <circle cx="12" cy="12" r="8" />
    <path d="M12 1.5v4.5M12 18v4.5M1.5 12h4.5M18 12h4.5" />
    <circle cx="12" cy="12" r="1.4" />
  `,

  /*
   * 手稳度:靶心 + 一个**偏离圆心**的点。
   *
   * 那一点偏出去是故意的:这件测的正是"你以为停住了,其实指针还在飘",
   * 一个端端正正落在圆心的点会把它说反。内圈用虚线,和空态装置面同一种记号。
   */
  'steadiness-test': `
    <circle cx="12" cy="12" r="8.5" />
    <circle cx="12" cy="12" r="4" stroke-dasharray="2 2.5" />
    <circle cx="13.4" cy="10.8" r="1.3" fill="currentColor" stroke="none" />
  `,

  /** 涂鸦板:一笔随手画出来的线。 */
  'doodle-test': `
    <path d="M3.5 15.5c2-6 4-6 5.5-1.5s3 3 4-1 2.5-5 5.5-5.5" />
  `,

  /** 灵敏度换算器:几行数值 + 一个向下的箭头(换算是"从这些推出那个")。 */
  'sensitivity-converter': `
    <path d="M4 8h7M4 12h9M4 16h5" />
    <path d="M16.5 5.5v13M16.5 18.5l-2.5-3M16.5 18.5l2.5-3" />
  `,

  /** 坏点检测:一格里填满的那个格子,就是它让你找的东西。 */
  'dead-pixel-test': `
    <path d="M4 4h6v6H4zM14 4h6v6h-6zM4 14h6v6H4z" />
    <path d="M14 14h6v6h-6z" fill="currentColor" stroke="none" />
  `,

  /** 刷新率目视:一块拖着一串速度线的方块。 */
  'refresh-visual-test': `
    <rect x="12.5" y="7.5" width="8" height="8" rx="1" />
    <path d="M3 11.5h6M3 15.5h4M3 7.5h5" opacity="0.45" />
  `,
};
